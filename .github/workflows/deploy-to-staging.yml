name: Deploy to staging
on:
  workflow_call:

jobs:
  deploy-to-staging:
    runs-on: ubuntu-latest
    environment: staging
    steps:
      - name: Checkout
        uses: actions/checkout@v3
    
      - name: Echo "In staging area"
        run: |
            echo "In staging area"